---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.ts
  - tsconfig.json
  - src/env.d.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "npm run build completes with zero errors"
    - "npx astro check reports zero TypeScript errors"
    - "Tailwind v4 utility classes are processed (no @astrojs/tailwind present)"
    - "All runtime dependencies installed: three, gsap, lenis, tailwindcss, @tailwindcss/vite"
  artifacts:
    - path: "astro.config.ts"
      provides: "Astro project config with Tailwind v4 Vite plugin and sitemap"
      contains: "@tailwindcss/vite"
    - path: "tsconfig.json"
      provides: "TypeScript strict mode configuration"
      contains: "strict"
    - path: "package.json"
      provides: "All project dependencies"
      contains: "\"astro\""
  key_links:
    - from: "astro.config.ts"
      to: "@tailwindcss/vite plugin"
      via: "vite.plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "tsconfig.json"
      to: "strict mode"
      via: "compilerOptions or extends"
      pattern: "strict"
---

<objective>
Scaffold a new Astro 6 project with TypeScript strict mode and integrate Tailwind CSS v4 via the `@tailwindcss/vite` Vite plugin. Install all project dependencies upfront (Three.js, GSAP, Lenis) to prevent package.json churn in later phases.

Purpose: Establish the runnable project skeleton that every subsequent phase builds on. Nothing can be implemented until `npm run build` passes.
Output: A clean Astro 6 project at the root of the working directory with `astro.config.ts` configured, strict TypeScript active, and all dependencies installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro 6 project with TypeScript strict mode</name>
  <files>package.json, astro.config.ts, tsconfig.json, src/env.d.ts</files>
  <read_first>
    - /home/josue/Documents/projects/personal-website/.planning/phases/01-foundation/01-RESEARCH.md (standard stack versions, installation commands, project structure)
  </read_first>
  <action>
The project is being created inside /home/josue/Documents/projects/personal-website/ which already contains .planning/ and CLAUDE.md. Scaffold Astro into this directory without overwriting existing files.

Run in /home/josue/Documents/projects/personal-website/:

```bash
npm create astro@latest . -- --template minimal --typescript strict --no-install --no-git --yes
```

If the CLI refuses to scaffold into a non-empty directory, use the force flag or manually create the required files. The Astro minimal template creates:
- package.json (with astro as dependency)
- astro.config.mjs (rename to astro.config.ts)
- tsconfig.json (must extend "astro/tsconfigs/strict")
- src/env.d.ts (Astro type references)
- src/pages/index.astro (placeholder)

After scaffolding, rename astro.config.mjs to astro.config.ts if needed. Replace its contents with:

```typescript
// astro.config.ts
import { defineConfig } from 'astro/config'
import tailwindcss from '@tailwindcss/vite'
import sitemap from '@astrojs/sitemap'

export default defineConfig({
  site: 'https://josue.aparcedo.org',
  integrations: [sitemap()],
  vite: {
    plugins: [tailwindcss()],
  },
})
```

Verify tsconfig.json extends "astro/tsconfigs/strict" OR has "strict": true under compilerOptions. If the scaffolded tsconfig only has "extends": "astro/tsconfigs/base", change it to "astro/tsconfigs/strict".

Example correct tsconfig.json:
```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"]
}
```

NAS build note: If running a build, set CARGO_TARGET_DIR is not relevant here (that's Rust). For npm/node builds on NAS, no special path is needed — Astro builds are fast and use the local node_modules.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/personal-website && cat tsconfig.json | grep -E '"strict"' && cat astro.config.ts | grep "tailwindcss"</automated>
  </verify>
  <done>astro.config.ts exists with tailwindcss() in vite.plugins; tsconfig.json extends astro/tsconfigs/strict or has "strict": true</done>

  <acceptance_criteria>
    - `cat tsconfig.json` contains either `"astro/tsconfigs/strict"` or `"strict": true`
    - `cat astro.config.ts` contains `tailwindcss()` inside `vite: { plugins: [...] }`
    - `cat astro.config.ts` contains `sitemap()` inside `integrations: [...]`
    - `cat astro.config.ts` contains `site: 'https://josue.aparcedo.org'`
    - `cat astro.config.ts` does NOT contain `@astrojs/tailwind`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install all project dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - /home/josue/Documents/projects/personal-website/.planning/phases/01-foundation/01-RESEARCH.md (standard stack section — verified versions as of 2026-03-30)
  </read_first>
  <action>
Install all dependencies in one pass. Do NOT install @astrojs/tailwind (deprecated, v3 only). Do NOT install @studio-freight/lenis (old package name).

```bash
cd /home/josue/Documents/projects/personal-website

# Core Astro + Tailwind v4 integration
npm install tailwindcss @tailwindcss/vite @astrojs/sitemap

# Docs typography utility (harmless now, used in Phase 6)
npm install @tailwindcss/typography

# Runtime animation + 3D libraries (installed now to avoid churn)
npm install three gsap lenis

# Three.js TypeScript types (dev dependency)
npm install -D @types/three
```

After install, verify package.json contains all expected dependencies. The final package.json dependencies section must include:
- "astro": "^6.x.x"
- "tailwindcss": "^4.x.x"
- "@tailwindcss/vite": "^4.x.x"
- "@tailwindcss/typography": "^0.5.x"
- "@astrojs/sitemap": "^3.x.x"
- "three": "^0.183.x"
- "gsap": "^3.14.x"
- "lenis": "^1.3.x"

devDependencies must include:
- "@types/three": "^0.183.x"

After install, run `npm run build` to confirm the base scaffold builds without errors. The build will produce a dist/ directory. A successful build = zero errors in terminal output.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/personal-website && cat package.json | grep -E '"tailwindcss"|"@tailwindcss/vite"|"three"|"gsap"|"lenis"' && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>`npm run build` exits 0 with zero errors; package.json contains tailwindcss, @tailwindcss/vite, three, gsap, lenis</done>

  <acceptance_criteria>
    - `cat package.json` contains `"tailwindcss"` (v4.x, NOT v3)
    - `cat package.json` contains `"@tailwindcss/vite"` (NOT `"@astrojs/tailwind"`)
    - `cat package.json` contains `"three"`, `"gsap"`, `"lenis"`
    - `cat package.json` does NOT contain `"@astrojs/tailwind"` or `"@studio-freight/lenis"`
    - `npm run build` exits with code 0
    - `npx astro check` exits with code 0 (zero TypeScript errors)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run both commands and verify zero errors:
1. `cd /home/josue/Documents/projects/personal-website && npx astro check`
2. `cd /home/josue/Documents/projects/personal-website && npm run build`
3. `grep -E "@astrojs/tailwind|@studio-freight" package.json` — must return empty (these packages must NOT be installed)
</verification>

<success_criteria>
- `npm run build` exits 0 — Astro project builds without errors
- `npx astro check` exits 0 — TypeScript strict mode active, zero type errors
- `package.json` contains tailwindcss@4.x, @tailwindcss/vite, three, gsap, lenis
- `astro.config.ts` uses @tailwindcss/vite in vite.plugins (NOT @astrojs/tailwind in integrations)
- `tsconfig.json` has strict TypeScript enabled
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Astro version installed
- All dependency versions from package.json
- Any scaffolding decisions made (flags used, files renamed)
- Build output (zero errors confirmed)
</output>
