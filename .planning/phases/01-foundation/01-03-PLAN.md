---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/layouts/BaseLayout.astro
  - src/layouts/MainLayout.astro
  - src/layouts/DocsLayout.astro
  - src/components/ui/FilmGrain.astro
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/projects.astro
  - src/pages/interests.astro
  - src/pages/contact.astro
  - src/pages/docs/index.astro
autonomous: false
requirements:
  - FOUND-04
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "Visiting / renders a dark page with visible film grain and Playfair Display heading"
    - "Visiting /docs renders a light/neutral page with NO film grain"
    - "Both layouts render correctly at 375px (mobile), 768px (tablet), and 1280px (desktop)"
    - "Tailwind v4 utility classes (text-primary, bg-surface-2, font-display) are visually applied"
    - "Console shows zero errors on both / and /docs"
    - "Google Fonts (Playfair Display, Space Grotesk, JetBrains Mono) are loaded and rendering"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "HTML shell with Google Fonts, global CSS import, meta tags"
      contains: "fonts.googleapis.com"
    - path: "src/layouts/MainLayout.astro"
      provides: "Immersive layout wrapping BaseLayout with dark background + FilmGrain"
      contains: "FilmGrain"
    - path: "src/layouts/DocsLayout.astro"
      provides: "Clean docs layout wrapping BaseLayout with light/neutral background"
      contains: "docs-shell"
    - path: "src/components/ui/FilmGrain.astro"
      provides: "SVG feTurbulence grain overlay, position:fixed, pointer-events:none"
      contains: "feTurbulence"
    - path: "src/pages/index.astro"
      provides: "Landing page using MainLayout with token demo content"
      contains: "MainLayout"
    - path: "src/pages/docs/index.astro"
      provides: "Docs landing using DocsLayout"
      contains: "DocsLayout"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "import in frontmatter"
      pattern: "global.css"
    - from: "src/layouts/MainLayout.astro"
      to: "src/components/ui/FilmGrain.astro"
      via: "import and component use"
      pattern: "FilmGrain"
    - from: "src/pages/index.astro"
      to: "src/layouts/MainLayout.astro"
      via: "import and layout component"
      pattern: "MainLayout"
    - from: "src/pages/docs/index.astro"
      to: "src/layouts/DocsLayout.astro"
      via: "import and layout component"
      pattern: "DocsLayout"
---

<objective>
Build the layout layer and all page stubs. BaseLayout owns the HTML shell; MainLayout adds dark immersive styling with film grain; DocsLayout adds a clean light layout. All seven page routes get stub content that proves the layout chain works and visually demonstrates the design tokens.

Purpose: This is the last plan in Phase 1 — it closes the loop from scaffold to tokens to rendered visual output. Every subsequent phase imports these layouts and builds within them.
Output: 3 layout files, 1 FilmGrain component, 6 page stubs. Running `npm run dev` shows dark luxury aesthetic on `/` and clean docs aesthetic on `/docs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interfaces>
Token utility classes generated by Plan 02 tokens.css @theme block.
Use these in layout/page files — they are guaranteed to exist after Plan 02.

Color utilities:
  bg-primary        maps to background: #a855f7
  bg-surface-2      maps to background: #1a1a1a
  bg-bg             maps to background: #0a0a0a
  text-primary      maps to color: #a855f7
  text-primary-soft maps to color: #c4b5fd
  text-text         maps to color: #e4e4e7
  text-text-muted   maps to color: #a1a1aa
  text-text-accent  maps to color: #c4b5fd

Typography utilities:
  font-display  maps to font-family: 'Playfair Display', Georgia, serif
  font-body     maps to font-family: 'Space Grotesk', system-ui, sans-serif
  font-mono     maps to font-family: 'JetBrains Mono', 'Courier New', monospace

Border radius:
  rounded-sm maps to border-radius: 2px
  rounded    maps to border-radius: 4px

CSS variables for use in style blocks:
  var(--color-primary), var(--color-bg), var(--color-text)
  var(--font-display), var(--font-body), var(--font-mono)
  var(--radius), var(--radius-sm)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build layout chain and FilmGrain component</name>
  <files>
    src/layouts/BaseLayout.astro,
    src/layouts/MainLayout.astro,
    src/layouts/DocsLayout.astro,
    src/components/ui/FilmGrain.astro
  </files>
  <read_first>
    - /home/josue/Documents/projects/personal-website/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: BaseLayout to MainLayout inheritance chain; Pattern 4: Film Grain SVG feTurbulence; Pitfall 5: DocsLayout background)
    - /home/josue/Documents/projects/personal-website/.planning/phases/01-foundation/01-CONTEXT.md (D-04 fonts, D-05 film grain, D-09 through D-14 visual decisions)
  </read_first>
  <action>
Create `src/layouts/` and `src/components/ui/` directories if they don't exist, then create these four files:

#### FILE: src/layouts/BaseLayout.astro

Owns the HTML shell, head, Google Fonts link, global.css import, meta tags. Does NOT set any background color — each layout handles its own visual context. The preconnect hints before the Google Fonts link are required (Pitfall 3 mitigation — prevents DNS lookup penalty on LCP).

```astro
interface Props {
  title: string
  description?: string
}
const { title, description = 'Software engineer, builder, creator.' } = Astro.props
```

Full file content (write exactly as shown):

The file is an HTML document with lang="en". Head contains: charset UTF-8, viewport meta, title tag showing `{title} — Josue Aparcedo`, description meta. Then two preconnect links (fonts.googleapis.com and fonts.gstatic.com with crossorigin). Then the Google Fonts stylesheet link with this exact URL:
`https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;0,900;1,400&family=Space+Grotesk:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;700&display=swap`

Import global CSS in the frontmatter component script (NOT a `<link>` tag — Astro/Vite does not serve /src/ paths as static assets at runtime): `import '../styles/global.css'`. Body contains a slot. No background color on body.

#### FILE: src/components/ui/FilmGrain.astro

SVG feTurbulence film grain. Key properties (D-05): position fixed, inset 0, z-index 9999, pointer-events none, opacity 0.04, mix-blend-mode overlay. The SVG uses a filter with id "grain-noise" containing feTurbulence (type fractalNoise, baseFrequency 0.65, numOctaves 3, stitchTiles stitch) and feColorMatrix (type saturate, values 0). A rect fills 100% width and height with the filter applied. The wrapping div has aria-hidden="true". Both the div and the svg must have pointer-events: none.

#### FILE: src/layouts/MainLayout.astro

Imports BaseLayout and FilmGrain. Props interface: `{ title: string, description?: string }`. Renders BaseLayout wrapping a div with class "main-site-shell". Inside the shell: FilmGrain component, then slot. The main-site-shell div uses CSS with: background-color var(--color-bg), color var(--color-text), min-height 100vh, position relative.

CRITICAL: Dark background goes on .main-site-shell div, NOT on body. This prevents the dark background from bleeding into DocsLayout (Pitfall 5).

#### FILE: src/layouts/DocsLayout.astro

Imports BaseLayout only (NO FilmGrain import — docs has zero film grain). Props interface: `{ title: string, description?: string }`. Renders BaseLayout wrapping a div with class "docs-shell". Inside: aside.docs-sidebar (empty, Phase 6 adds content) and main.docs-content with slot.

CSS for docs-shell: display flex, min-height 100vh, background-color #fafafa, color #111111, font-family var(--font-body).
CSS for docs-sidebar: width 280px, min-height 100vh, background-color #f4f4f5, flex-shrink 0, padding 2rem 1rem.
CSS for docs-content: flex 1, padding 2rem 3rem, max-width 860px.
Mobile media query (max-width 768px): docs-shell flex-direction column; docs-sidebar width 100%, min-height auto, padding 1rem; docs-content padding 1rem.

After creating all four files, run `npm run build` to confirm zero errors.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/personal-website && ls src/layouts/ && ls src/components/ui/ && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 files exist; npm run build exits 0; BaseLayout.astro contains Google Fonts link; FilmGrain.astro contains feTurbulence; MainLayout has .main-site-shell with dark bg; DocsLayout has .docs-shell with #fafafa bg</done>

  <acceptance_criteria>
    - `ls src/layouts/` shows BaseLayout.astro, MainLayout.astro, DocsLayout.astro
    - `ls src/components/ui/` shows FilmGrain.astro
    - `grep "fonts.googleapis.com" src/layouts/BaseLayout.astro` returns a match
    - `grep "Playfair Display" src/layouts/BaseLayout.astro` returns a match in the Google Fonts URL
    - `grep "preconnect" src/layouts/BaseLayout.astro` returns 2 matches (both preconnect hints)
    - `grep "feTurbulence" src/components/ui/FilmGrain.astro` returns a match
    - `grep "pointer-events: none" src/components/ui/FilmGrain.astro` returns a match
    - `grep "position: fixed" src/components/ui/FilmGrain.astro` returns a match
    - `grep "mix-blend-mode: overlay" src/components/ui/FilmGrain.astro` returns a match
    - `grep "FilmGrain" src/layouts/MainLayout.astro` returns a match (component imported and used)
    - `grep "var(--color-bg)" src/layouts/MainLayout.astro` returns a match (dark bg on wrapper)
    - `grep "FilmGrain" src/layouts/DocsLayout.astro` returns empty (FilmGrain must NOT be in DocsLayout)
    - `grep "#fafafa" src/layouts/DocsLayout.astro` returns a match (light background)
    - `npm run build` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create page stubs for all routes</name>
  <files>
    src/pages/index.astro,
    src/pages/about.astro,
    src/pages/projects.astro,
    src/pages/interests.astro,
    src/pages/contact.astro,
    src/pages/docs/index.astro
  </files>
  <read_first>
    - /home/josue/Documents/projects/personal-website/.planning/phases/01-foundation/01-CONTEXT.md (D-06: routes; D-07: landing page shows recruiter content; D-08: nav links)
    - /home/josue/Documents/projects/personal-website/.planning/phases/01-foundation/01-RESEARCH.md (test page example in Code Examples section)
    - /home/josue/Documents/projects/personal-website/src/layouts/MainLayout.astro (just created)
    - /home/josue/Documents/projects/personal-website/src/layouts/DocsLayout.astro (just created)
  </read_first>
  <action>
Create stub pages for all routes defined in D-06. Each stub uses the correct layout and displays design tokens visually so browser verification confirms tokens work. Do NOT add real content, animations, or Three.js — these are scaffolding pages only.

#### FILE: src/pages/index.astro

Uses MainLayout. Shows the name, tagline, and a design token demo for visual verification. The name heading uses font-family var(--font-display), font-size clamp(3rem, 10vw, 8rem), font-weight 900, color var(--color-primary). A label span uses font-family var(--font-mono), color var(--color-text-muted), text-transform uppercase, letter-spacing 0.15em. A code element uses font-family var(--font-mono), color var(--color-text-accent), background-color var(--color-surface-2), border-radius var(--radius). Label text: "Phase 1 — Foundation". Heading text: "Josue Aparcedo". Tagline text: "Software Engineer". Code text: "Design tokens active · Tailwind v4 · Astro 6". The page must use MainLayout with title="Home".

#### FILE: src/pages/about.astro

Uses MainLayout with title="About". Shows an h1 "About" in font-display, color primary, and a paragraph "Full about section coming in Phase 5." in font-body, color text-muted. The page is centered vertically via flexbox on a min-height 100vh container.

#### FILE: src/pages/projects.astro

Same pattern as about.astro. title="Projects", h1 "Projects", p "Full projects showcase coming in Phase 5."

#### FILE: src/pages/interests.astro

Same pattern. title="Interests", h1 "Interests", p "Full interests section coming in Phase 5."

#### FILE: src/pages/contact.astro

Same pattern. title="Contact", h1 "Contact", p "Contact section coming in Phase 5."

#### FILE: src/pages/docs/index.astro

Uses DocsLayout with title="Docs". Shows h1 "Knowledge Base" and two paragraphs: "Full docs section with sidebar navigation and search coming in Phase 6." and "This page renders on a white/neutral background — no film grain, no dark aesthetic." The import path for DocsLayout is `../../layouts/DocsLayout.astro` (two levels up from docs/).

After creating all 6 page files, run `npm run build`. The build must complete with zero errors.
  </action>
  <verify>
    <automated>cd /home/josue/Documents/projects/personal-website && ls src/pages/ && ls src/pages/docs/ && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 page files exist; npm run build exits 0; dist/ contains index.html and docs/index.html</done>

  <acceptance_criteria>
    - `ls src/pages/` shows index.astro, about.astro, projects.astro, interests.astro, contact.astro
    - `ls src/pages/docs/` shows index.astro
    - `grep "MainLayout" src/pages/index.astro` returns a match
    - `grep "DocsLayout" src/pages/docs/index.astro` returns a match
    - `grep "var(--font-display)" src/pages/index.astro` returns a match (tokens used in index)
    - `grep "var(--color-primary)" src/pages/index.astro` returns a match
    - `npm run build` exits with code 0
    - `ls dist/` shows index.html (landing page built)
    - `ls dist/docs/` shows index.html (docs page built)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 Foundation scaffold:
    - Astro 6 project with TypeScript strict mode (Plan 01)
    - Tailwind v4 via @tailwindcss/vite (Plan 01)
    - Design tokens: 11 color tokens, 3 font tokens, 2 radius tokens in @theme block (Plan 02)
    - BaseLayout to MainLayout to DocsLayout chain (Plan 03, Task 1)
    - FilmGrain SVG feTurbulence overlay on dark pages only (Plan 03, Task 1)
    - 6 page stubs showing design tokens visually (Plan 03, Task 2)
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` in /home/josue/Documents/projects/personal-website
    2. Visit http://localhost:4321 in Brave browser
       - Dark near-black background (#0a0a0a)
       - "Josue Aparcedo" in large Playfair Display serif, purple (#a855f7)
       - "Software Engineer" in Space Grotesk, muted gray
       - Token demo code block with dark surface background
       - Subtle film grain texture visible over the dark background (very subtle, opacity 0.04)
    3. Open DevTools console — verify ZERO errors
    4. Resize browser to 375px width — verify no layout breakage, text still readable
    5. Visit http://localhost:4321/docs
       - WHITE/NEUTRAL background — NOT dark
       - NO film grain texture visible
       - "Knowledge Base" heading
       - Two-column layout visible (sidebar left, content right) even if sidebar is empty
    6. Visit http://localhost:4321/about, /projects, /interests, /contact
       - Each should have dark background with Playfair Display heading in purple
    7. Check font rendering: Playfair Display (serif), Space Grotesk (geometric sans), JetBrains Mono (monospace code font) should all be visibly distinct from each other
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Before checkpoint:
1. `npm run build` — zero errors
2. `npx astro check` — zero TypeScript errors
3. `grep "FilmGrain" src/layouts/DocsLayout.astro` — must return empty (grain NOT in docs)
4. `grep "@astrojs/tailwind" package.json` — must return empty (wrong integration not installed)

At checkpoint:
- Visual: dark luxury aesthetic on /
- Visual: clean white/neutral on /docs
- Console: zero errors on both pages
- Responsive: no breakage at 375px
</verification>

<success_criteria>
- `npm run build` exits 0 — full project builds without errors
- `npx astro check` exits 0 — TypeScript strict mode, zero type errors
- Browser: / shows dark background, Playfair Display heading in purple, visible film grain
- Browser: /docs shows white/neutral background, NO film grain
- Browser: console shows zero errors on both pages
- Browser: layout holds at 375px, 768px, 1280px without breakage
</success_criteria>

<output>
After completion (including checkpoint approval), create `.planning/phases/01-foundation/01-03-SUMMARY.md` documenting:
- Layout chain created (BaseLayout to MainLayout/DocsLayout)
- FilmGrain implementation details (SVG feTurbulence, opacity used, z-index)
- Page stubs created and their routes
- Checkpoint verification results (what was confirmed in browser)
- Any visual adjustments made (e.g., film grain opacity tweaked from 0.04 to another value)
</output>
