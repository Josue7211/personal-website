# Policy Index

Repo policy is lightweight for now:

- `ROADMAP.md` is the single roadmap truth.
- `HANDOFF.json` is the machine-readable handoff packet.
- `docs/handoff/` is the human-readable handoff trail.
- Do not recreate `.planning` or GSD workflow files in this repo.
