# V1 Roadmap Bootstrap

## Summary

- Repo migrated from `.planning` to `.memd`-first project memory.
- Root `ROADMAP.md` is now the single roadmap source of truth.
- Root `HANDOFF.json` is now the machine-readable handoff packet.
- Full memd-style docs structure exists under `docs/`.
- A1 Foundation is treated as complete; B1 Scroll Infrastructure is next.

## Current Truth

- Local app is running at `http://127.0.0.1:4321/`.
- Homepage and content pages are still stubs.
- Docs section is still a shell layout.
- Astro warns about Vite 8 drift against Astro 6 expectations.

## Next

- Start `B1: Scroll Infrastructure`.
- Resolve the Vite drift before it becomes a hidden source of dev/build weirdness.
- Replace stubs with the real immersive landing and docs system.
